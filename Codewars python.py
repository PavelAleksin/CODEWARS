# def square_digits(num):
    # return (''.join(str(int(i)**2) for i in str(num)))

# def find_it(seq):
    # s = [int(seq.count(i)) for i in seq]
    # for n in s:
        # if n % 2 != 0:
            # return seq[s.index(n)]

